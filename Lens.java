class Lens {
    String type;
    int focalLength; // in millimeters

    Lens(String Type, int FocalLength) {
        System.out.println("Created a Lens");
        type = Type;
        focalLength = FocalLength;
    }
}
